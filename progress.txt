## Task: Add hook detection and clip boundary optimization

### Implementation Summary:
Implemented a comprehensive hook detection and clip boundary optimization system to improve clip quality and viewer retention by ensuring clips start with strong hooks and end on complete thoughts.

### Changes Made:

#### 1. Created Hook Detection Module (server/src/analysis/hooks.ts)
- **Hook Analysis Function (`analyzeHook`)**: Analyzes the first 3 seconds of clips to detect hook quality
  - Detects 4 types of strong hooks:
    - **Question hooks**: Starts with "What", "How", "Why", etc. (base score: 85)
    - **Reaction hooks**: Starts with "Oh my god", "No way", "Wait", etc. (base score: 90)
    - **Action hooks**: Starts with "Watch", "Check this", "Look", etc. (base score: 80)
    - **Statement hooks**: Declarative sentence with capitalization (base score: 70)
  - Applies penalties for weak starts:
    - Mid-sentence starts: -30% penalty
    - Silent starts (>0.5s delay): -40% penalty
    - Filler word starts (um, uh, like): -20% penalty
  - Bonus for high word density (confident delivery): +10%

#### 2. Clip Boundary Optimization Function (`optimizeClipBoundaries`)
- **Start Boundary Optimization**: Aligns clip start to sentence boundaries
  - Searches backward up to 2 seconds to find sentence start
  - Avoids starting with filler words
  - Ensures clip captures full context
- **End Boundary Optimization**: Ensures clips end on complete thoughts
  - Aligns to sentence boundaries
  - Adds 0.5s breathing room after last word
  - Prevents mid-sentence cuts
- **Duration Enforcement**: Ensures clips meet minimum 8-second duration

#### 3. Hook Scoring Integration
- **`applyHookScore` function**: Applies hook score to base clip score
  - Default weight: 20% (configurable)
  - Boosts clips with strong hooks
  - Formula: baseScore + (hookScore * hookWeight)
- **`filterWeakHooks` function**: Filters out clips with poor hooks
  - Default threshold: hookScore < 30 are filtered
  - Configurable threshold for different quality standards

#### 4. Updated Fusion Types (server/src/analysis/fusion.ts)
- Extended `SignalMoment` interface to include:
  - `hookScore?: number` - Hook quality score (0-100)
  - `boundaryOptimization?` - Adjusted clip boundaries with reasoning

### Test Coverage:

#### Created Comprehensive Test Suite (server/src/analysis/__tests__/hooks.test.ts)
24 tests covering all functionality:

**Hook Detection Tests (12 tests):**
- Question hook detection
- Reaction hook detection
- Action hook detection
- Statement hook detection
- Mid-sentence start penalties
- Silent start penalties
- Filler word start penalties
- Empty transcript handling
- High word density bonus

**Boundary Optimization Tests (7 tests):**
- Start alignment to sentence boundaries
- End alignment to sentence boundaries
- Breathing room after last word
- Avoiding filler word starts
- Minimum duration enforcement
- Empty transcript graceful handling
- Max adjustment limit respect

**Scoring Tests (5 tests):**
- Hook score boosting base score
- Score bounds enforcement
- Custom hook weight application
- Weak hook filtering
- Custom threshold filtering

**Test Results**: All 24 tests passing ✓

### Integration Points:

The hook detection system is designed to integrate into the pipeline at these points:

1. **During Caption Stage**: After transcription is complete, run `analyzeHook()` on each clip
2. **In Fusion Stage**: Apply hook scores to moments using `applyHookScore()`
3. **Before Extraction**: Optimize boundaries with `optimizeClipBoundaries()`
4. **Final Filtering**: Remove weak hooks with `filterWeakHooks()`

### Configuration Options:

The system provides several configurable parameters:
- `hookWeight`: Impact of hook score on final score (default: 0.2)
- `minHookScore`: Threshold for filtering weak hooks (default: 30)
- `maxAdjustment`: Maximum seconds to adjust boundaries (default: 2)
- `hookWindow`: Seconds analyzed for hook detection (default: 3)

### Files Created:
- server/src/analysis/hooks.ts (298 lines)
- server/src/analysis/__tests__/hooks.test.ts (396 lines)

### Files Modified:
- server/src/analysis/fusion.ts (added hookScore and boundaryOptimization to SignalMoment)

### Benefits:

1. **Improved Viewer Retention**: Clips start with engaging hooks that capture attention
2. **Better Clip Quality**: Aligned boundaries ensure complete thoughts and natural pacing
3. **Reduced Weak Clips**: Automatic filtering removes clips with poor openings
4. **Data-Driven Optimization**: Uses transcript analysis for intelligent boundary detection
5. **Flexible Configuration**: Adjustable thresholds and weights for different use cases

### Next Steps (for future integration):

To fully integrate this feature into the pipeline:
1. Wire hook analysis into caption stage after transcription
2. Update analyze stage to apply hook scores to moments
3. Add hook filtering before clip extraction
4. Expose configuration options in job settings
5. Add UI controls for hook sensitivity settings

### Notes:
- All code passes ESLint with zero warnings or errors
- Comprehensive test coverage ensures reliability
- Designed to be backward compatible (optional hookScore field)
- Gracefully handles missing transcript data
- Performance optimized to analyze only first 3 seconds of clips
- The env object now exports Supabase credentials instead of S3-compatible storage credentials
- OpenAI API key has been added for AI functionality
- Default values remain empty strings to maintain consistency with other configuration

---

## Task: Add multi-face handling and speaker prioritization

### Changes Made:
- Enhanced server/src/extraction/reframe.ts with comprehensive multi-face handling and speaker prioritization
- Added new interfaces: DetectedFace, FaceTrack, FaceSelectionStrategy
- Extended ReframeConfig to support faceStrategy and audioMoments parameters
- Implemented IOU-based face tracking for consistent face identification across frames
- Added three face selection strategies:
  1. 'largest': Selects face with largest average bounding box area
  2. 'speaker': Correlates faces with audio moments for speaker detection
  3. 'center': Selects face closest to frame center
- Implemented speaker prioritization using audio moment correlation (within 0.5s window)
- Added face persistence tracking to avoid jumping between different faces
- Created comprehensive test suite in server/src/extraction/reframe.test.ts with 19 test cases

### Files Modified:
- server/src/extraction/reframe.ts
- server/src/extraction/reframe.test.ts (new file)

### Test Coverage:
- IOU calculation tests (perfect overlap, no overlap, partial overlap)
- Face track building tests (single track, multiple tracks, temporal separation)
- Largest face selection tests (single track, multiple tracks, averaging)
- Speaker track selection tests (audio correlation, proximity weighting, fallback)
- Center track selection tests
- Integration tests for multi-face handling scenarios

### Implementation Details:
1. **Face Tracking**: Uses Intersection Over Union (IOU) algorithm with 0.3 threshold
2. **Temporal Continuity**: Only tracks faces within 1 second of each other
3. **Speaker Detection**: Correlates faces with audio moments within 0.5s window
4. **Proximity Weighting**: Linear weighting based on temporal distance to audio peaks
5. **Graceful Fallback**: Falls back to largest face when no audio correlation exists

### Notes:
- Face detection stub remains in place (detectFacesForReframe returns empty array)
- Ready for MediaPipe integration when face detection is implemented
- All tests passing (19/19)
- No new linting errors introduced
- Implementation matches PRD requirements for multi-face handling and speaker prioritization

---

## Task: Add transition effects for multi-clip compilations

### Changes Made:
- Added transition types and configuration to pipeline types system
- Implemented enhanced concatenation with transition support in clipper module
- Created new Effects pipeline stage for applying transitions to multi-clip compilations
- Comprehensive test coverage for all transition types and edge cases

### Files Modified:
- server/src/pipeline/types.ts - Added TransitionType, TransitionConfig, and CompilationConfig types
- server/src/extraction/clipper.ts - Added concatenateClipsWithTransitions function with support for cut, flash, zoom-in, and zoom-out transitions
- server/src/pipeline/stages/effects.ts - New pipeline stage for applying transitions (NEW FILE)

### Files Created:
- server/src/pipeline/stages/effects.ts - Effects stage implementation
- server/src/pipeline/stages/effects.test.ts - Comprehensive test suite for effects stage
- server/src/extraction/clipper.test.ts - Test suite for transition functions

### Implementation Details:

#### Transition Types Supported:
1. **Cut** - Instant transition (no effect, simple concatenation)
2. **Flash** - White flash effect between clips using xfade filter
3. **Zoom-in** - Zoom in effect on transition using xfade filter
4. **Zoom-out** - Zoom out effect on transition using xfade filter

#### Key Features:
- Flexible transition configuration per clip boundary
- Default transition type and duration settings
- Fallback to simple concatenation when all transitions are 'cut'
- Single clip optimization (no unnecessary processing)
- Automatic selection of best available clips (captioned > reframed)
- File validation before processing
- Comprehensive error handling

#### FFmpeg Filter Complex:
The implementation uses FFmpeg's xfade filter for smooth transitions between video clips:
- Calculates offset times based on clip durations
- Builds complex filter chains for video transitions
- Concatenates audio streams separately
- Outputs high-quality MP4 with h264/aac encoding

#### Testing:
- 22 tests covering all transition types and configurations
- Edge case handling (no clips, single clip, missing files)
- Configuration validation and preference testing
- All tests passing with 100% success rate
- Linting clean on all new files

### Integration Points:
The Effects stage can be added to the pipeline orchestrator between the caption and upload stages:
1. Download → Analyze → Extract → Reframe → Caption → **Effects** → Upload

### Next Steps for Integration:
To use this feature in production:
1. Add EffectsStage to the pipeline orchestrator
2. Configure transition preferences in job settings
3. Update upload stage to handle compiled clip if available
4. Add UI controls for transition selection

### Notes:
- Transitions are applied only when multiple clips are being compiled
- Single clips bypass the compilation step for efficiency
- The system gracefully handles missing transition configurations
- All code follows existing patterns and coding standards

---

## Task: Build platform connection UI in Settings page

### Changes Made:
- Created new PlatformConnection component (src/components/settings/PlatformConnection.tsx)
  - Reusable component for managing platform OAuth connections
  - Supports Twitch (via Supabase OAuth), YouTube (via Google OAuth), and TikTok
  - Shows connection status with visual indicators (checkmark icon when connected)
  - Handles connect/disconnect flows with proper error handling
  - Displays loading states during OAuth operations

- Updated Settings page (src/pages/Settings.tsx)
  - Replaced placeholder platform cards with PlatformConnection components
  - Added descriptive text for Connected Accounts section
  - Integrated with Zustand store for connection status tracking

- Enhanced AuthCallback page (src/pages/AuthCallback.tsx)
  - Added platform parameter handling from OAuth redirect URL
  - Updates store with correct connection status based on platform
  - Redirects to Settings page after non-Twitch platform connections
  - Fixed React hook linting warning

- Wrote comprehensive tests (src/components/settings/PlatformConnection.test.tsx)
  - 9 test cases covering rendering, platform support, and component structure
  - Tests verify button states, accessibility, and platform-specific behavior
  - All tests passing with happy-dom test environment

### Files Modified:
- src/components/settings/PlatformConnection.tsx (new file)
- src/components/settings/PlatformConnection.test.tsx (new file)
- src/pages/Settings.tsx
- src/pages/AuthCallback.tsx
- vitest.config.ts (changed from jsdom to happy-dom)

### Technical Details:
- OAuth Providers:
  - Twitch: uses 'twitch' provider via Supabase Auth
  - YouTube: uses 'google' provider with YouTube upload scope
  - TikTok: placeholder (no provider configured yet)

- Connection Status Tracking:
  - Stored in Zustand store (user.twitchConnected, user.tiktokConnected, user.youtubeConnected)
  - Persisted to localStorage via Zustand persist middleware

- Disconnect Behavior:
  - Twitch: full sign out from Supabase (clears all connections)
  - Other platforms: updates connection status in store only

### Testing:
- All 9 tests pass successfully
- Linting passes with no errors
- Test dependencies installed: vitest, @testing-library/react, @testing-library/jest-dom, @testing-library/user-event, jsdom, happy-dom

### Notes:
- TikTok OAuth integration is not yet configured (button is disabled)
- YouTube OAuth requires backend configuration in Supabase
- Future enhancements could include displaying connected account details (username, avatar)
- OAuth callback URLs must be configured in Supabase dashboard

---

## Task: Wire split-screen layout for gaming content

### Changes Made:
- Added `splitScreen: boolean` setting to job schema in server/src/routes/jobs.ts
  - Updated ProcessingJob interface to include splitScreen in settings
  - Added splitScreen to Zod schema with default value of false
  - Ensured splitScreen is included in job creation logic
- Implemented split-screen detection and logic in server/src/pipeline/stages/reframe.ts
  - Created `shouldUseSplitScreen()` helper function to check if split-screen should be used
  - Imported `createSplitScreen` from reframe.ts extraction module
  - Added conditional logic to use split-screen when enabled and dual sources are available
  - Implemented fallback to standard reframe when dual sources are not available
- Wrote comprehensive tests for split-screen functionality
  - Added split-screen tests to server/src/pipeline/stages/reframe.test.ts
  - Created new test file server/src/extraction/reframe.test.ts with tests for the extraction module
  - All 30 split-screen related tests pass successfully

### Files Modified:
- server/src/routes/jobs.ts
- server/src/pipeline/stages/reframe.ts
- server/src/pipeline/stages/reframe.test.ts
- server/src/extraction/reframe.test.ts (new file)

### Implementation Details:
- The `createSplitScreen()` function was already fully implemented in reframe.ts but was never called
- Split-screen uses FFmpeg vstack filter to stack facecam (35% of height) on top of gameplay (65%)
- When splitScreen setting is true, the pipeline checks for dual video sources in context.metadata
- If dual sources (facecamPath and gameplayPath) are available, creates split-screen layout
- If dual sources are not available, falls back to standard center crop reframe
- Maintains target aspect ratio (default 9:16 for vertical videos)

### Acceptance Criteria Met:
✓ Split-screen layout is generated when splitScreen setting is true
✓ Facecam appears on top, gameplay on bottom (when dual sources available)
✓ Output maintains 9:16 aspect ratio
✓ Falls back to normal reframe if splitScreen is false or dual sources unavailable
✓ Tests pass successfully
✓ No new linting errors introduced

### Notes:
- For MVP, dual video sources are not yet extracted from VODs, so split-screen will fall back to standard reframe
- Future enhancement: Extract separate facecam and gameplay streams from VOD before reframe stage
- Future enhancement: Auto-detect gaming content by VOD category to enable split-screen automatically
- The infrastructure is now in place and ready for when dual source extraction is implemented

---

## Task: Add filler word removal

### Changes Made:
- Implemented filler word removal functionality in the transcription module
- Added `removeFillers` and `customFillerWords` configuration options to `TranscribeConfig` interface
- Created `removeFillerWords()` function that processes transcription results to remove common filler words
- Integrated filler removal into both `transcribeWithWhisperAPI()` and `transcribeWithLocalWhisper()` functions
- Implemented smart removal logic that:
  - Removes default filler words: um, uh, umm, uhh, like, you know, i mean, sort of, kind of, basically, actually, literally
  - Preserves filler words after sentence-ending punctuation (., !, ?)
  - Preserves filler words with high confidence (>= 0.95) indicating emphasis
  - Adjusts timestamps to close gaps < 1 second
  - Preserves natural pauses > 1 second
  - Supports custom filler word lists
- Created comprehensive test suite with 14 test cases covering all edge cases

### Files Modified:
- server/src/captions/transcribe.ts (added removeFillerWords function and updated TranscribeConfig)
- server/src/captions/transcribe.test.ts (new file with comprehensive tests)

### Test Results:
- All 14 tests passing
- Test coverage includes:
  - Default filler word removal
  - Timestamp adjustment
  - Natural pause preservation
  - Sentence-start word preservation
  - Emphasis detection
  - Custom filler words
  - Multiple segments handling
  - Empty segment removal
  - Case-insensitive matching
  - Multi-word phrases

### Notes:
- Feature is opt-in via `removeFillers: true` config flag
- Default behavior unchanged when flag is not set
- No new linting errors introduced
- Implementation follows PRD requirements exactly

---

## Task: Add transcript-aware scoring with LLM analysis

### Changes Made:
- Created new transcript analysis module (server/src/analysis/transcript.ts) with LLM-powered scoring
- Implemented TranscriptMoment interface to track clip-worthy moments from speech analysis
- Added analyzeTranscript() function that uses OpenAI GPT-4 to identify funny, intense, quotable, emotional, and narrative moments
- Updated signal fusion module to support transcript signals as a fourth signal type
- Modified FusionConfig to include transcript weight (default: 0.3, balanced with chat and audio at 0.3 each)
- Enhanced title generation to prioritize transcript excerpts when available
- Lowered minScore threshold from 30 to 20 to accommodate weighted single-signal moments
- Created comprehensive test suites for both transcript analysis and fusion with transcript support

### Files Created:
- server/src/analysis/transcript.ts - Core transcript analysis module with LLM integration
- server/src/analysis/transcript.test.ts - 17 tests covering all transcript analysis functionality
- server/src/analysis/fusion.test.ts - 15 tests covering signal fusion with transcript support

### Files Modified:
- server/src/analysis/fusion.ts - Added transcript signal support and updated fusion logic
- package.json - Added vitest, @vitest/ui, jsdom, @testing-library/jest-dom, and @testing-library/react as dev dependencies

### Implementation Details:
- Transcript analysis uses sliding windows (30s default) with overlapping context
- LLM analyzes chunks and scores moments 0-100 based on clip-worthiness
- Five moment types identified: funny, intense, quotable, emotional, narrative
- Confidence scoring (0-1) indicates LLM certainty about each moment
- Deduplication logic merges overlapping moments, keeping higher-scored ones
- Fusion convergence bonus applied when transcript aligns with chat/audio/clips
- All TypeScript linting rules satisfied with proper type safety

### Test Coverage:
- Transcript analysis: 17 tests covering API integration, JSON parsing, error handling, and moment types
- Fusion with transcripts: 15 tests covering signal combination, weighting, title generation, and edge cases
- All 32 tests passing successfully

### Notes:
- Uses OpenAI GPT-4 Turbo for fast, cost-effective analysis
- Designed to integrate seamlessly with existing chat and audio analysis
- Transcript signals enhance clip detection accuracy through multi-modal fusion
- Ready for integration into the pipeline stages for end-to-end clip generation

---

## Task: Integrate MediaPipe Face Detection for smart reframing

### Changes Made:

1. **Installed MediaPipe Dependency**
   - Added `@mediapipe/tasks-vision@0.10.32` to server dependencies
   - This package provides the FaceDetector and FilesetResolver for face detection

2. **Implemented Face Detection in reframe.ts** (server/src/extraction/reframe.ts)
   - Integrated MediaPipe Face Detection into existing multi-face handling infrastructure
   - Extracts video frames at 2 FPS using FFmpeg for analysis
   - Uses MediaPipe BlazeFace short-range model for accurate face detection
   - For each detected face:
     - Calculates face center coordinates
     - Generates crop keyframes to keep face centered in frame
     - Clamps coordinates to valid bounds
   - Falls back gracefully to center crop if face detection fails or no faces found
   - Added `loadImageFromFile()` helper function to convert frames to ImageData format for MediaPipe
   - Proper cleanup of temporary frames after processing
   - Integrated with existing IOU-based face tracking for multi-face handling
   - Supports face selection strategies (largest, speaker, center)

3. **Enabled Face Tracking in Pipeline** (server/src/pipeline/stages/reframe.ts)
   - Changed `faceTracking` parameter from `false` to `true` in reframeVideo call
   - Updated console log message to reflect "smart reframing" instead of "center crop"
   - Face tracking now runs automatically for all clips in the pipeline
   - Maintains split-screen logic for gaming content

4. **Added Comprehensive Tests** (server/src/extraction/reframe.test.ts)
   - Combined multi-face handling tests with MediaPipe integration tests
   - Test coverage includes:
     - IOU calculation tests (perfect overlap, no overlap, partial overlap)
     - Face track building tests (single track, multiple tracks, temporal separation)
     - Face selection strategy tests (largest, speaker, center)
     - Aspect ratio validation
     - Face detection fallback behavior
     - Smoothing parameter validation
     - Keyframe generation and structure
     - Different target aspect ratios (9:16, 1:1, 16:9, 4:5)
     - MediaPipe integration verification
     - Split-screen layout tests
   - All tests pass successfully

5. **Updated Existing Tests** (server/src/pipeline/stages/reframe.test.ts)
   - Modified test expectations to reflect face tracking being enabled
   - Updated expected config to have `faceTracking: true`

### Technical Implementation Details:

**Face Detection Flow:**
1. Extract frames at 2 FPS from input video using FFmpeg
2. Initialize MediaPipe FaceDetector with BlazeFace short-range model
3. Process each frame to detect faces
4. Build face tracks using IOU-based tracking
5. Select best track based on strategy (largest, speaker, or center)
6. For detected faces:
   - Get bounding box coordinates
   - Calculate face center position
   - Generate crop keyframe to center face in output
7. Apply smoothing to keyframes for fluid camera motion
8. Build FFmpeg crop filter with interpolation
9. Clean up temporary files

**Fallback Strategy:**
- If MediaPipe initialization fails: falls back to center crop
- If no faces detected in any frame: falls back to center crop
- If frame processing fails: continues to next frame
- Graceful error handling throughout

**Performance Considerations:**
- Runs at 2 FPS (analyzes 1 frame every 0.5 seconds)
- Minimal overhead for short clips (typical 10-60 second clips)
- Temporary frames cleaned up after processing
- Model loaded from CDN (no local storage required)

### Files Modified:
- server/src/extraction/reframe.ts - Implemented MediaPipe face detection with multi-face handling
- server/src/pipeline/stages/reframe.ts - Enabled face tracking in pipeline
- server/src/extraction/reframe.test.ts - Combined multi-face and MediaPipe tests
- server/src/pipeline/stages/reframe.test.ts - Updated existing tests
- server/package.json - Added @mediapipe/tasks-vision dependency

### Acceptance Criteria Met:
✓ MediaPipe FaceDetector successfully detects faces in test clips
✓ Multi-face handling with IOU-based tracking implemented
✓ Face selection strategies (largest, speaker, center) working
✓ Crop keyframes are generated with face center coordinates
✓ Face stays centered in 9:16 output video throughout clip
✓ Smoothing prevents jittery crop motion (0.7 smoothing factor applied)
✓ Falls back gracefully if no faces detected (reverts to center crop)
✓ All tests pass
✓ No linting errors in new/modified code

### Notes:
- Face detection uses BlazeFace short-range model which is optimized for faces close to camera (ideal for streaming content)
- The 2 FPS analysis rate balances performance and accuracy - sufficient for smooth tracking
- Existing smoothKeyframes() and buildCropFilter() functions handle interpolation between keyframes
- Integration is fully backward compatible - if face detection fails, system falls back to center crop
- Multi-face handling allows prioritization based on audio correlation for speaker detection

---

## Task: Add B-roll insertion using LLM transcript analysis

### Overview:
Implemented a complete B-roll insertion system that uses LLM (OpenAI/Claude) to analyze video transcripts and automatically insert relevant stock footage from Pexels at appropriate moments. This enhances videos with supplemental visual content that supports what's being discussed.

### Changes Made:

#### 1. LLM Integration (`server/src/lib/llm.ts`)
- Created LLM client supporting both OpenAI and Anthropic Claude
- Analyzes video transcripts to identify B-roll insertion opportunities
- Generates structured suggestions with timestamps, durations, search queries, and context
- Validates and normalizes LLM responses to ensure consistent output
- Clamps B-roll durations between 3-10 seconds for optimal viewing

#### 2. Pexels API Integration (`server/src/lib/pexels.ts`)
- Implemented Pexels API client for searching and downloading stock footage
- Supports filtering by orientation (landscape/portrait/square) and quality
- Smart video file selection based on preferred dimensions
- Handles video downloads and saves them to local filesystem
- Filters for MP4 format and applies minimum duration requirements

#### 3. B-roll Pipeline Stage (`server/src/pipeline/stages/broll.ts`)
- New pipeline stage that runs after caption stage
- Loads transcription from JSON file saved by caption stage
- Analyzes transcript using configured LLM provider
- Downloads B-roll videos based on LLM suggestions
- Uses FFmpeg overlay filter to composite B-roll over main video
- Reduces main video opacity to 40% during B-roll segments
- Scales B-roll footage to match main video dimensions
- Supports configurable max B-roll insertions (default: 5)
- Gracefully handles failures and returns original video if B-roll fails

#### 4. Pipeline Integration (`server/src/pipeline/orchestrator.ts`)
- Added B-roll stage to pipeline after caption stage
- Modified caption stage to always save transcription JSON for B-roll analysis
- Store transcription paths in pipeline context metadata
- B-roll stage reads settings from job configuration:
  - `bRoll`: boolean flag to enable/disable B-roll
  - `maxBRolls`: maximum number of B-roll insertions per video
  - `outputFormat`: determines orientation for stock footage search
- Falls back to captioned clips if B-roll is disabled or fails
- Stores B-roll clips in context metadata for upload stage

#### 5. Upload Stage Update (`server/src/pipeline/stages/upload.ts`)
- Modified to prioritize B-roll clips over captioned clips
- Upload order: brollClips → captionedClips → reframedClips → extractedClips
- Ensures final videos with B-roll insertions are uploaded to storage

#### 6. Environment Variables (`server/src/lib/env.ts`)
- Added `CLAUDE_API_KEY` for Anthropic Claude API access
- Added `PEXELS_API_KEY` for Pexels stock footage API
- Added `LLM_PROVIDER` to configure LLM choice ('openai' or 'claude')
- Defaults to 'openai' provider for backward compatibility

#### 7. Comprehensive Tests
- `server/src/lib/llm.test.ts`: Tests for LLM transcript analysis
  - Validates suggestion structure and required fields
  - Tests duration clamping (3-10 seconds)
  - Handles empty transcriptions
  - Supports both OpenAI and Claude providers
  - Error handling for unsupported providers
- `server/src/lib/pexels.test.ts`: Tests for Pexels API client
  - Best video file selection logic
  - MP4 format filtering
  - Dimension matching
  - Search configuration
  - Duration filtering
- `server/src/pipeline/stages/broll.test.ts`: Tests for B-roll pipeline stage
  - Skip behavior when disabled
  - Empty insertions when no suggestions found
  - Configuration validation (maxBRolls, opacity, orientation)

### Files Created:
- `server/src/lib/llm.ts` (275 lines)
- `server/src/lib/llm.test.ts` (138 lines)
- `server/src/lib/pexels.ts` (165 lines)
- `server/src/lib/pexels.test.ts` (108 lines)
- `server/src/pipeline/stages/broll.ts` (260 lines)
- `server/src/pipeline/stages/broll.test.ts` (145 lines)

### Files Modified:
- `server/src/lib/env.ts`: Added CLAUDE_API_KEY, PEXELS_API_KEY, LLM_PROVIDER
- `server/src/pipeline/orchestrator.ts`: Integrated B-roll stage, updated caption stage to save transcriptions
- `server/src/pipeline/stages/upload.ts`: Prioritize B-roll clips for upload

### Technical Details:

#### LLM Prompt Strategy:
The system sends the full transcript with timestamps to the LLM and asks it to identify moments where B-roll would enhance the video. The LLM returns JSON with:
- Timestamp (seconds from start)
- Duration (suggested length of B-roll)
- Search query (for Pexels API)
- Context (explanation of why this B-roll makes sense)

#### FFmpeg B-roll Overlay:
Uses complex filter graph to overlay multiple B-roll segments:
1. Creates base layer with reduced opacity (40%)
2. Scales each B-roll to match video dimensions
3. Uses `enable` expression to show B-roll only during specified time ranges
4. Overlays B-roll on base layer with temporal control
5. Copies audio from main video (no B-roll audio)

#### Configuration Requirements:
To use B-roll insertion, jobs need:
```typescript
{
  settings: {
    bRoll: true,
    maxBRolls: 3,  // optional, default 5
    outputFormat: 'vertical'  // determines B-roll orientation
  }
}
```

Environment variables needed:
- `OPENAI_API_KEY` or `CLAUDE_API_KEY` (depending on LLM_PROVIDER)
- `PEXELS_API_KEY`
- `LLM_PROVIDER` (optional, defaults to 'openai')

### Testing Results:
- All B-roll tests passed
- Tests validate configuration, structure, and error handling
- Tests use mock data and expect API call failures without real keys
- Lint checks passed for all new files (0 errors, 0 warnings)

### Integration Points:
- Caption stage now saves transcription JSON by default
- B-roll stage is optional and controlled by job settings
- Falls back gracefully if API keys are missing or calls fail
- Upload stage automatically uses B-roll clips when available

### Future Enhancements:
- Add B-roll audio mixing options
- Support custom opacity configurations per job
- Add B-roll preview generation
- Cache LLM analyses for reprocessing
- Add analytics for B-roll engagement metrics

---

## Task: Add animation effects to ASS caption generator

### Changes Made:
- Added CaptionAnimations interface with configurable bounce, glow, fadeIn effects and intensity levels
- Updated generateTikTokASS() to accept animations configuration parameter
- Implemented bounce effect: words scale from 100% to 105-120% (based on intensity) and back
- Implemented glow pulse effect: outline width increases from 3 to 4-6 (based on intensity) and back
- Implemented fade-in effect: words fade in from alpha 80-200 to 0 (fully opaque) based on intensity
- Added intensity levels: 'subtle' (105% scale, 4px outline), 'medium' (110% scale, 5px outline), 'strong' (120% scale, 6px outline)
- Default behavior: all animations enabled with 'medium' intensity
- Animations use ASS \t() transform tags for smooth frame-accurate animations
- Updated CaptionConfig interface in caption.ts to include animations in styling options

### Files Modified:
- server/src/captions/transcribe.ts (added CaptionAnimations interface, updated generateTikTokASS function)
- server/src/pipeline/stages/caption.ts (added CaptionAnimations import and to styling config)

### Files Created:
- server/src/captions/transcribe.test.ts (13 new tests covering all animation scenarios)

### Tests Added:
- Test for bounce animation generation
- Test for glow pulse animation generation
- Test for fade-in animation generation
- Test for all animations combined
- Test for intensity levels (subtle, medium, strong)
- Test for animations disabled
- Test for word-by-word highlighting preservation
- Integration tests in caption.test.ts for animation config passing through pipeline

### Test Results:
- All 27 caption-related tests passing
- All 13 new transcribe tests passing
- Animation tags correctly generated in ASS output
- Proper ASS syntax for \t() transforms and \fade() effects

### Notes:
- Animations are hardware-accelerated by ASS renderer (no performance impact)
- Animation timings are calculated per-word based on actual word duration
- Default configuration enables all animations for best visual impact
- Can be fully disabled by setting all animation flags to false
- ASS format natively supports these effects, no external dependencies needed

---

## Task: Wire viewer clips into pipeline and honor all job settings

### Changes Made:
1. **Enhanced TwitchClient to fetch viewer clips for VODs**
   - Added TwitchClip interface with all necessary properties including vod_offset
   - Implemented getClipsForVOD() method that fetches all clips for a specific VOD
   - Handles pagination automatically to retrieve all clips
   - Filters clips by video_id to ensure only relevant clips are returned

2. **Wired viewer clips into pipeline orchestrator**
   - Added function to convert Twitch clips to ViewerClip format for signal fusion
   - Modified analyzeStageWrapper to fetch viewer clips from Twitch API using VOD ID
   - Passes twitchVodId through pipeline context metadata
   - Gracefully handles failures when fetching viewer clips

3. **Honored all job settings in signal fusion**
   - Implemented dynamic signal weights based on job settings:
     - chatAnalysis: 0.4 if enabled, 0 if disabled
     - audioPeaks: 0.4 if enabled (or 0.8 if chat disabled), 0 if disabled
     - clips weight: always 0.2
   - Added sensitivity mapping to minScore thresholds:
     - low: 50 (only high-confidence moments)
     - medium: 30 (default balance)
     - high: 15 (more permissive)
   - Pass minDuration and maxDuration from job settings to fusion config

4. **Updated analyze stage to accept full fusion configuration**
   - Changed AnalyzeStageConfig to extend Partial<FusionConfig>
   - Modified analyze function to pass all fusion config options (not just weights)
   - Properly separates audioOutputPath from fusion config

5. **Added comprehensive tests**
   - Created twitch.test.ts with tests for getClipsForVOD functionality
   - Added tests for pagination handling and VOD filtering
   - Added tests in analyze.test.ts for honoring job settings
   - Tests verify minDuration, maxDuration, minScore, and full fusion config

### Files Modified:
- server/src/lib/twitch.ts
- server/src/lib/twitch.test.ts (new)
- server/src/pipeline/orchestrator.ts
- server/src/pipeline/stages/analyze.ts
- server/src/pipeline/stages/analyze.test.ts

### Technical Details:
- Viewer clips are fetched using Twitch API based on VOD time range
- Only clips with vod_offset are converted to ViewerClip format
- Signal fusion now respects all job settings for weights, thresholds, and durations
- Pipeline context includes twitchVodId in metadata for clip fetching
- Tests pass and linting is clean (only pre-existing warnings remain)

### Notes:
- faceReactions setting is not implemented (feature doesn't exist yet)
- autoCaptions is already honored in the caption stage
- outputFormat is already honored in the reframe stage
- The implementation maintains backward compatibility with empty viewer clips array
